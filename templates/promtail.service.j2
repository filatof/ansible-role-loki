[Unit]
Description=Promtail Log Collector
After=network.target

[Service]
User={{ promtail_user }}
Group={{ promtail_group }}
ExecStart={{ promtail_install_dir }}/promtail --config.file={{ promtail_config_dir }}/promtail.yaml
Restart=always

[Install]
WantedBy=multi-user.target