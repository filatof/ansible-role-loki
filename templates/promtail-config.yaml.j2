server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://{{ loki_url | default('localhost:3100') }}/loki/api/v1/push

scrape_configs:
  - job_name: syslog
    static_configs:
      - targets:
          - localhost
        labels:
          job: syslog
          host: "{{ inventory_hostname }}"
          __path__: /var/log/syslog
      - targets:
          - localhost
        labels:
          job: auth
          host: "{{ inventory_hostname }}"
          __path__: /var/log/auth.log
      - targets:
          - localhost
        labels:
          job: kern
          host: "{{ inventory_hostname }}"
          __path__: /var/log/kern.log
