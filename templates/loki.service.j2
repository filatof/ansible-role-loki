[Unit]
Description=Loki Log Aggregation
After=network.target

[Service]
User={{ loki_user }}
Group={{ loki_group }}
ExecStart={{ loki_install_dir }}/loki --config.file={{ loki_config_dir }}/loki.yaml
Restart=always

[Install]
WantedBy=multi-user.target